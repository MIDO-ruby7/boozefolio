
<div>
  <h3 class="ml-2">
    <div class="badge badge-accent text-stone-600">
      <%= t('.new') %>
    </div>
    <%= t('.title') %>
  </h3>
  <div class="grid gap-3 grid-cols-2 md:grid-cols-5 grid-rows-3 p-4">
    <% if @items.present? %>
      <%= render @items %>
    <% else %>
      <p><%= t('.no_image') %></p>
    <% end %>
  </div>
</div>

